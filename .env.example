# GitHub fine-grained PAT with read-only Discussions access.
# Powers the converge/diverge reaction feature (/api/ensemble).
# Contributors do NOT need this for writing or previewing posts.
GITHUB_TOKEN=
